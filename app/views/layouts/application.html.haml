!!!
%html
  %head
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "Control centro de enseñanza"
    %meta{content: content_for?(:description) ? yield(:description) : "Control centro de enseñanza", name: "description"}/
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %header= render 'layouts/navigation'
    %main{role: "main"}
      = render 'layouts/messages'
      = yield
      - if Rails.env.development?
        .alert.alert-warning
          = params
          %br
          - if current_user != nil
            = "User id: " + current_user.id.to_s