%ul.nav.nav-pills
  %li{:role => "presentation", class: "#{'active' if params[:type] == 'month'} "}
    = link_to 'Mes',user_chart_url(user_id: @user.id,type: 'month'), remote: true
  %li{:role => "presentation" , class: "#{'active' if params[:type] == 'day'}"}
    = link_to 'Dia',user_chart_url(user_id: @user.id,type: 'day'), remote: true

= line_chart  @procedures_hash
